CREATE TABLE marathon (
   Place INTEGER PRIMARY KEY,
   Time TIME,
   Pace TIME,
   GroupPlace INTEGER, 
   GroupLabel CHAR(10),
   Age INTEGER,
   Sex CHAR(1),
   BIBNumber INTEGER,
   FirstName CHAR(30),
   LasName CHAR(30),
   Town CHAR(30),
   State CHAR(2)
);
